<%= form_for([@restaurant, @special_menu, @food_type]) do |form| %>
  <% if @food_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food_type.errors.count, "error") %> prohibited this food_type from being saved:</h2>

      <ul>
      <% @food_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
  <% @restaurant.food_items.each do |food_item| %>
    <%= check_box_tag 'food_type[food_item_ids][]', food_item.id %>
    <%= label_tag food_item.name %>
    <br>
  <% end %>
  </div>

  <div class="field">
    <%= form.label :have_to_select_one %>
    <%= form.check_box :have_to_select_one %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
